const READ = "read";
const WRITE = "write";
const DELETE = "delete";
const ALL = "all";
const NONE = "none";

const config =
{
    READ,
    WRITE,
    DELETE,
    ALL,
    NONE
};

export default config;
